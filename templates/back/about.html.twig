{% extends 'back/base.html.twig' %}

{% block title %}{{parent()}} - A propos{% endblock %}

{% block content %}



<div class="about-wrapper">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    <div class="about">
        <h1>{{ title }}</h1>
        <p>{{ about }}</p>
    </div>
    
</div>
{% endblock %}
